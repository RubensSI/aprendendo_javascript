// declaração de variávies do jogo
let canvas, context, ALTURA, LARGURA, frames = 0;

function clique() {}

function main() {}
  // definir o tamanho da janela
  // obtem a altura da janela
  ALTURA = window.innerWidth;

  // abtem a largura da janela
  LARGURA = window.innerHeight;

  // definie a largura a altura da janela caso ela seja maior ou igual a 500px
  if (LARGURA <= 600) {
    LARGURA = 600;
    ALTURA = 600;
  }
function roda() {}

function atualiza() {}

function desenha() {}

// inicializa o jogo
main();